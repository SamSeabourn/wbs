---
interface Props {
	buttonType: 'primary' | 'secondary';
}

const { buttonType } = Astro.props;
---

<button class=`btn btn-xs btn-${buttonType} sm:btn-sm md:btn-md lg:btn-lg xl:btn-xl`
	><slot /></button
>
